
//import java.io.*;

public class Main {
    public static void main(String[] args) {











        /*
        BufferedReader bufferedReader = new BufferedReader(new FileReader("B:\\PROGRAMPROJECT\\GettingStarted\\src\\Main.java"));
        String line = bufferedReader.readLine();
        BufferedWriter bufferedWriter = new BufferedWriter(new FileWriter("a.txt"));
        BufferedWriter是缓冲输入流，意思是当你调用BufferedWriter的write方法时候。数据是先写入到缓冲区里，
        并没有直接写入到目的文件里。你必须调用BufferedWriter的flush()方法。
        这个方法会刷新一下该缓冲流，也就是会把数据写入到目的文件里。
        或者你可以调用BufferedWriter的close()方法，该方法会在关闭该输入流之前先刷新一下该缓冲流。也会把数据写入到目的文件里
             bufferedReader.close();

        */


    }
}
